import type {
  DatasetIngest as PrismaDatasetIngest,
  DatasetVersion as PrismaDatasetVersion,
} from "@fiery/db";

export type DatasetIngest = PrismaDatasetIngest;
export type DatasetVersion = PrismaDatasetVersion;
